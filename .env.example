# HTTP listener (server.ts), independent of config.mongodb's app.port
PORT=3003

# Dev environment (NODE_ENV=dev)
DEV_APP_PORT=3003
DEV_DB_HOST=localhost
DEV_DB_PORT=27017
DEV_DB_NAME=shopDEV

# Production environment (NODE_ENV=pro)
PRO_APP_PORT=3000
PRO_DB_HOST=localhost
PRO_DB_PORT=27017
PRO_DB_NAME=shopPRO
